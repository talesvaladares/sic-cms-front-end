import styled from "styled-components";

export const MainContainer = styled.div`
  /* max-width: 1000px; */
  height: calc(100vh - 60px);
  margin: 0 auto;

  padding: 0 0.5rem;

  overflow: hidden;
`;